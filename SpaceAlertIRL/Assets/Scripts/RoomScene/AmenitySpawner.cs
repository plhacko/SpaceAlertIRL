using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class AmenitySpawner : MonoBehaviour
{
    [SerializeField]
    private Room Room;
    [SerializeField]
    private GameObject PowerGeneratorIcon;
    [SerializeField]
    private GameObject EnergyPoolIcon;
    [SerializeField]
    private GameObject EnergyNodeIcon;


    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
